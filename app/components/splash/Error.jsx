import React from 'react';
import Splash from './Splash.jsx';

const Error = () => (
  <div>
    <Splash title={'There seems to be a problem'} subtitle={'Please try again'} />
  </div>
);

export default Error;
